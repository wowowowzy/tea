<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.tea.mapper.QuestionMapper">


    <select id="queryQuestionById" resultType="com.example.tea.entity.dto.Question.QuestionDTO">
        select * from tea_choice_question tcq
            left join tea_choice_option tco on tcq.question_id = tco.question_id
        where tcq.question_id = #{questionId}
    </select>
    <select id="getAnswer" resultType="java.lang.String">
        select tco.option_label from tea_choice_question tcq
            left join tea_choice_option tco
                on tcq.correct_option_id = tco.option_id
        where tcq.question_id = #{id}
    </select>
</mapper>
